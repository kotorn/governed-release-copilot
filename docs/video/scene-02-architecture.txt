The architecture follows one governed path: change evidence becomes a ReleaseChangeV1 payload, validation checks the schema and canonical changeHash, and a deterministic policy decides whether the request can continue. A ready request may produce a structured AI draft, but it still stops at human approval. Only an approved decision reaches SharePoint knowledge. Copilot Studio then answers from that approved source with citations, while Work IQ may contribute organizational context.
